export declare class Speedometer {
    private _history;
    private _start;
    private _window;
    constructor(windowSize?: number);
    readonly bytesPerSecond: number;
    readonly currentSpeed: string;
    eta(targetBytes: number): number;
    record(bytes: number): void;
    private _getRange;
}
